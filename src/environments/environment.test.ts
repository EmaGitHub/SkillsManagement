import { Environment } from 'src/app/shared/domain/utils/environment.model';

// const apiBaseUrl = 'https://testincommerce.ecredit.it/apiectx';
const apiBaseUrl = '';

export const environment: Environment = {
  production: true,
  debuglevel: 'error',
  apiUrl: ''
};
